import 'package:flutter/material.dart';

class DriverBioScreen extends StatelessWidget {
  const DriverBioScreen({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Text('hello world'),
      )
    );
  }
}
